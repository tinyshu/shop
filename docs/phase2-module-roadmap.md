# 阶段二：按模块细切片推进路线图

> 脚手架（去品牌、配置隔离、冒烟）已完成。本文件是二次开发的**推进顺序与过关标准**。  
> 工作流：`design.md` → `tasks.md` → 只改 `new_shop/` → 过关验证。规范见 `.steerin/feature-designer.md` / `feature-task-maker.md`。  
> 缺口 ID 对照：[todo-gaps.md](./todo-gaps.md)、[payment-reliability.md](./payment-reliability.md)。

---

## 1. 工作节奏

```text
选切片 → design.md → tasks.md → 编码（new_shop）→ 过关验证 → 下一切片
```

| 约定 | 说明 |
|------|------|
| 切片粒度 | 0.5～2 天可关闭；禁止一次做完整个支付/履约域 |
| 文档落点 | `docs/features/[模块]/[版本]/{server\|web\|uniapp}/` |
| 表变更 | 加字段/新表优先；主表名与主外键尽量不动 |
| 表通用化 | 夹在对应业务切片，不单开整库重构 |
| 禁止 | 跳过 `tasks.md` 直接大改；未过关就开下一 P0/P1 切片 |

---

## 2. 模块总序

```text
M0 行业开关底座
 → M1 支付可靠性 P0
 → M2 履约/退款 P1
 → M3 登录安全 P1
 → M4 物流字段通用化 P2（可与 M2 后并行）
 → M5 按客户加项（非默认必做）
```

---

## 3. 细切片清单与过关标准

### M0 行业开关底座

| 切片 ID | 内容 | todo-gaps | 文档目录 | 过关标准 |
|---------|------|-----------|----------|----------|
| **M0-1** | 配置键约定 + server 读取封装 | GEN-03 前置 | [features/industry-config](./features/industry-config/) | 键有种子/SQL；业务可读布尔/枚举默认值；缺键不崩 |
| **M0-2** | 用户审核可关（GEN-03） | GEN-03 | 同上后续版本 | 关：注册/登录默认通过、可下单；开：与现网卡审核一致 |
| **M0-3** | 管理端开关编辑（极简） | — | web 端 design | **v0.3.0 已实现**：后台「功能开关」改 feature.* |

**当前进度**：M0、payment v0.2.0、fulfillment v0.1.0（M2-1）/ **v0.2.0（M2-2）** 已实现。PAY-04/05 暂缓。下一刀可为 M2-3 或 M3。

### M1 支付可靠性 P0

| 切片 ID | 内容 | todo-gaps | 过关标准 |
|---------|------|-----------|----------|
| **M1-1** | 回调金额与订单金额校验 | PAY-02 | 金额不符拒写已支付；有日志 |
| **M1-2** | 条件更新幂等入账（无流水表） | PAY-03 | **v0.2.0 已实现**；流水表刻意后置 |
| **M1-3** | 掉单主动查单补 `status=1` | PAY-01 | **v0.2.0 已实现**（管理端同步 + 可配置定时） |

### M2 履约 / 退款 P1

| 切片 ID | 内容 | todo-gaps | 过关标准 |
|---------|------|-----------|----------|
| **M2-1** | 人工「标记退款完成」专用接口 | FUL-06 | **v0.1.0 已实现** |
| **M2-2** | 发货/取消条件更新 | FUL-05 | **v0.2.0 已实现** |
| **M2-3** | 小程序确认收货路径对齐 | FUL-02 | 前端打到真实后端路由且状态正确 |
| **M2-4** | 微信自动退款闭环 | PAY-05 / FUL-01 | 可报价加项；同意后退款到账+状态+库存规则按 design |

### M3 登录安全 P1

| 切片 ID | 内容 | todo-gaps | 过关标准 |
|---------|------|-----------|----------|
| **M3-1** | `session_key` 仅服务端 | AUTH-01 | 响应不再回传 session_key；解密仍可用 |
| **M3-2** | phone 索引 + 唯一（方案 A） | AUTH-02 | 登录查询走索引；并发不轻易双用户 |
| **M3-3** | openid 主路径（方案 B，可选） | AUTH-02 | 按 design 合并规则可回归 |

### M4 物流表通用化 P2

| 切片 ID | 内容 | todo-gaps | 过关标准 |
|---------|------|-----------|----------|
| **M4-1** | 快递公司 + 运单号字段；配送员可空 | FUL-04 | 快递模式可发货无配送员；城配开关仍可用配送员 |

依赖 M0 的 `feat` 物流模式开关。

### M5 按客户加项（默认不做）

| 示例 | todo-gaps | 说明 |
|------|-----------|------|
| 多规格购物车/下单 | GEN-01 | 合同需要再开切片 |
| 余额支付闭环 | 见 05-account | 非零售默认 |
| 月结 UI / 批量结算强化 | 03b | 小 B 客户再开 |

---

## 4. 如何开口推进

- 「按 roadmap 做 M0-2」/「只做 M1-1（PAY-02）」
- 「M0-1 design 通过，拆 tasks 并实现」
- 「本切片过关后再开下一片」

---

## 5. 相关索引

| 文档 | 用途 |
|------|------|
| [todo-gaps.md](./todo-gaps.md) | 缺口与优先级 |
| [payment-reliability.md](./payment-reliability.md) | 支付异常细节 |
| [learning-roadmap.md](./learning-roadmap.md) | 阶段一学习序；下一步链到本文 |
| [features/industry-config/](./features/industry-config/) | M0 设计与任务 |
| `.steerin/feature-designer.md` | design 写法 |
| `.steerin/feature-task-maker.md` | tasks 写法 |
